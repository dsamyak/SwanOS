/* ============================================================
 * SwanOS — Desktop Environment
 * Full OS-like desktop with windows, taskbar, icons, cursor.
 * ============================================================ */

#ifndef DESKTOP_H
#define DESKTOP_H

/* Entry point: runs the graphical desktop environment until exit */
void desktop_run(void);

#endif
